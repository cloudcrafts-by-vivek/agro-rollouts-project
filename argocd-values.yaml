server:
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - argocd.local
    paths:
      - /
    pathType: Prefix
  # In production, TLS would be configured here via cert-manager
  extraArgs:
    - --insecure # Required if SSL terminates at the NGINX ingress level

    